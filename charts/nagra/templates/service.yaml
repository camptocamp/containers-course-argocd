apiVersion: v1
kind: Service
metadata:
  name: {{ include "nagra.fullname" . }}
  labels:
    {{- include "nagra.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "nagra.selectorLabels" . | nindent 4 }}
